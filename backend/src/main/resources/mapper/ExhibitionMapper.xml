<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gallery.mapper.ExhibitionMapper">

    <select id="selectPageWithCategory" resultType="com.gallery.entity.Exhibition">
        SELECT e.*, ec.name AS category_name
        FROM exhibition e
        LEFT JOIN exhibition_category ec ON e.category_id = ec.id
        WHERE e.deleted = 0
        <if test="title != null and title != ''">
            AND e.title LIKE CONCAT('%', #{title}, '%')
        </if>
        <if test="categoryId != null">
            AND e.category_id = #{categoryId}
        </if>
        <if test="status != null">
            AND e.status = #{status}
        </if>
        ORDER BY e.create_time DESC
    </select>

</mapper>
